

# ============================================================
# Blockchain Evidence Preservation System - Docker Compose
# ============================================================

services:
  # ----------------------------------------------------------
  # PostgreSQL Database
  # ----------------------------------------------------------
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
     DATABASE_URL: postgresql+asyncpg://beps_user:beps_pass@db:5432/beps_db
     BLOCKCHAIN_RPC_URL: http://host.docker.internal:8545
     EVIDENCE_STORAGE_PATH: /app/evidence_storage
     JWT_ACCESS_TOKEN_EXPIRE_MINUTES: "480"
     JWT_REFRESH_TOKEN_EXPIRE_DAYS: "30"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U beps_user -d beps_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ----------------------------------------------------------
  # FastAPI Backend
  # ----------------------------------------------------------
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      db:
        condition: service_healthy
    env_file: ../.env
    environment:
      DATABASE_URL: postgresql+asyncpg://beps_user:beps_pass@db:5432/beps_db
      BLOCKCHAIN_RPC_URL: http://host.docker.internal:8545
      EVIDENCE_STORAGE_PATH: /app/evidence_storage
      JWT_ACCESS_TOKEN_EXPIRE_MINUTES: "480"
      JWT_REFRESH_TOKEN_EXPIRE_DAYS: "30"
    volumes:
      - evidence_storage:/app/evidence_storage
    ports:
      - "8000:8000"
  # ----------------------------------------------------------
  # Vue.js Frontend (served via Nginx)
  # ----------------------------------------------------------
  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "3000:80"

  # ----------------------------------------------------------
  # Hardhat Local Blockchain (Ethereum)
  # ----------------------------------------------------------
  hardhat:
    image: node:22-alpine
    working_dir: /app
    command: >
      sh -c "npm install --save-dev hardhat @nomicfoundation/hardhat-toolbox &&
             npx hardhat node --hostname 0.0.0.0 --port 8545"
    ports:
      - "8545:8545"
    volumes:
      - hardhat_data:/app

  # ----------------------------------------------------------
  # IPFS Node (optional)
  # ----------------------------------------------------------
  ipfs:
    image: ipfs/kubo:v0.28.0
    restart: unless-stopped
    environment:
      - IPFS_PROFILE=server
    volumes:
      - ipfs_data:/data/ipfs
    ports:
      - "4001:4001"  # Swarm
      - "5001:5001"  # API
      - "8080:8080"  # Gateway
    healthcheck:
      test: ["CMD", "ipfs", "id"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ----------------------------------------------------------
  # Alembic Migrations (run once at startup)
  # ----------------------------------------------------------
  migrations:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    depends_on:
      db:
        condition: service_healthy
    env_file: ../.env
    environment:
      DATABASE_URL: postgresql+asyncpg://beps_user:beps_pass@db:5432/beps_db
    command: ["alembic", "upgrade", "head"]
    volumes:
      - ../migrations:/app/migrations

volumes:
  postgres_data:
  evidence_storage:
  hardhat_data:
  ipfs_data:
